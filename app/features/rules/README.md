# Feature — rules (public site)

**Owner squad:** Player
**Status:** scaffold — folder shape and ownership agreed, no code yet.

## Purpose

"How to play", written for someone who has never played an RPG. The client set
the bar with "easy to start, rewarding to master" and named new players as an
explicit audience.

## Routes

_Planned._ Expected to be **static** (ADR-FE-003 default).

## APIs consumed

_Probably none._ If the copy lives in the page, say so here and skip `api/`
entirely — do not put content behind the Gateway until the content team
actually needs to edit it without a deploy.

## Test coverage

_Planned._

## Starting work here

Copy `app/features/_template/` and follow its README.
